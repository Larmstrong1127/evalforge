experiment_name: lr-5e5
model_name: microsoft/deberta-v3-base
# batch_size 32, not 64 — see lr-2e5.yaml for the CUDA OOM this fixes.
max_length: 512
batch_size: 32
learning_rate: 0.00005
warmup_ratio: 0.1
max_grad_norm: 1.0
max_epochs: 6
early_stopping_patience: 2
use_amp: true
seed: 42
